# GPU-окружение для index-build (эмбеддинги + Qdrant).
# Python 3.12 рекомендуется (как venv312 в rag_norm). На 3.13 — transformers>=4.46.
# torch+cuda ставится отдельной командой в setup_venv312.bat (иначе pip берёт win_amd64 CPU).

sentence-transformers>=3.0.1,<4
transformers>=4.46,<5
huggingface-hub>=0.24,<1

qdrant-client>=1.7.0
numpy>=1.26.0
tqdm>=4.65.0
