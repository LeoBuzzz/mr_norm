{
  "name": "planner_evidence",
  "version": "1",
  "role": "planner",
  "inputs": {
    "query": "string",
    "filters": "object",
    "profile": "fast|balanced|deep"
  },
  "output_contract": {
    "schema_version": "mr_planner_plan_v1",
    "required_fields": ["selected_tools", "routing_reasons"]
  },
  "prompt": "Select deterministic retrieval tools for the user query. Do not invent document names or point numbers."
}
