{
  "name": "reranker_evidence",
  "version": "1",
  "role": "reranker",
  "inputs": {
    "query": "string",
    "evidence": "array",
    "profile": "fast|balanced|deep"
  },
  "output_contract": {
    "schema_version": "mr_rerank_v1",
    "required_fields": ["ranked_chunk_ids"]
  },
  "prompt": "Reorder evidence chunks by relevance to the user query. Return JSON with key ranked_chunk_ids as an array of chunk_id strings from the provided evidence only. Return only one JSON object. No markdown fences, no reasoning text, no prose before or after JSON, no extra keys outside the contract."
}
