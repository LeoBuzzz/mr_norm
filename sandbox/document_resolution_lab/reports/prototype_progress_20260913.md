# Прогресс прототипа

## Сделано

- добавлен канонический реестр aliases и номеров в `src/mr_norm/config/document_alias_registry.json`;
- добавлен детерминированный resolver с приоритетом номера и точного alias;
- поддержаны формы `937 постановление`, `ФЗ 35`, `приказ 690`, `постановление правительства 86`, `приказ 903н`, `приказ 811`, `приказ 1070`;
- исправлены `ПТЭ`, `ПТЭ потребителей`, `правила ОТ`, `правила вывода в ремонт`, `методуказания по устойчивости`;
- для отсутствующего в каталоге документа № 1286 запрещён ложный fallback к другому документу;
- добавлены регрессионные тесты `tests/test_document_alias_registry.py`.

## Проверка

`15 passed` в наборе document resolution / norm lookup.

Связанный набор `test_query_understanding.py` имеет два ранее существовавших несовпадения контракта; они требуют отдельного решения и не включены в текущий патч.

## Следующий шаг

Проверить интеграцию фильтра `doc_id` в полном `norm_lookup`, затем расширить реестр формами номеров с `№`, дефисами, пробелами и упоминанием ведомства.
